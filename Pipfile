[[source]]
url = "https://pypi.org/simple"
verify_ssl = true
name = "pypi"

[packages]
requests = "*"
appdirs = "*"
click = "*"
python-comdirect-client = {editable = true, path = "."}

[dev-packages]

[requires]
python_version = "3.10"

[scripts]
install = "pipenv install -e ."
start = "pipenv run python src/__main__.py"
